package main

import (
	"fmt"
)

func main() {
	myString := "hello this is string"

	//转成ruin 、int和字节 的 slice
	//byteSlice := []byte(myString)

	//runeslice := []rune(myString)

	var a int = 1

	// fmt.Printf("format", a.(type))  .(type) user for switch
}
